import 'package:flutter/material.dart';

class FindDivision extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Center(
          child: Text("Find Division"),
        ),
      ),
    );
  }
}
